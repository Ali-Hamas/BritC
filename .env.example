# BritSync Assistant - Root Environment Variables (Optional)
# Most variables are configured in server/.env or in the Dashboard Settings

# Supabase (Used for Dashboard storage & auth)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
